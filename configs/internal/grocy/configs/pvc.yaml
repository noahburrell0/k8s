apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-config
  labels:
    app.kubernetes.io/name: grocy
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
